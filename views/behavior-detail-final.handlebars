<h4>Behavior Detail</h4><br/>
Student:
<h1>{{session.student_name}}</h1>

Grade: {{session.student_grade}}<br/>
<br><br>

<div class="behavior-container">

    <div class="item-behavior">
        <h5>Behavior</h5><br/>
        <span><input
                type="text"
                size="8"
                disabled="true"
                value="{{format_date studentBehavior.[0].Behaviors.[0].StudentBehavior.behavior_date}}"></span>&nbsp;
        <span class="behavior-description">{{studentBehavior.[0].Behaviors.[0].behavior_name}}</span>
        <input type="hidden" name="student_id" id="student_id" value="{{student.student_id}}"/>
        <br/><br/>
        <div class="note-behavior">{{studentBehavior.[0].Behaviors.[0].StudentBehavior.behavior_note}}</div>
        <br/>
        <br/>
    </div>
    <div class="item-behavior">
        <!--    WORKING-->
        <h5>Actions Taken</h5><br/>


        <span><input
                type="text"
                size="8"
                disabled="true"
                value="{{format_date consequence.action_date}}"></span>&nbsp;
        <span class="behavior-description">{{consequence.action_name}}</span>&nbsp;

        <br/><br/>
        <div class="note-behavior">{{consequence.action_note}}</div>
        <br/>
        <button class="css-button-sharp--sand"
                onclick="window.location.href='/api/user/showDashboard'">
            Back to Classroom
        </button>
        <br/><br/>
    </div>
</div>